{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@CreatorHub/auth": "workspace:*",
    "@CreatorHub/db": "workspace:*",
    "@CreatorHub/env": "workspace:*",
    "@fastify/cors": "^11.0.1",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "fastify": "^5.3.3",
    "fastify-type-provider-zod": "^6.1.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@CreatorHub/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
